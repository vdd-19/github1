git config --global user.name "vdd-19"
git config --global user.email "dhivyadharshini0401@gmail.com"
git clone
git init
--after day by use---

go to cmd
cd loc of git folder

git add name.f
git commit -m mistake
git push

git pull

--
git status
----
git branch
git checkout -b "name of newbranch"
git checkout main
git branch -M "changed name"
git branch -a

git push origin new branch

git stash
git stash pop